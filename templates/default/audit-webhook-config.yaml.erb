apiVersion: v1
kind: Config
clusters:
- name: <%= node['kubernetes']['cluster_name'] %>
  cluster:
    server: <%= @server %>
contexts:
- context:
    cluster: <%= node['kubernetes']['cluster_name'] %>
  name: audit-context
current-context: audit-context
